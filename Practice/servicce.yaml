apiVersion: v1
kind: Service
metadata:
  name: nginx-service

spec:
  type: LoadBalancer
  selector:
    env: demo-pod
  ports:
  - port: 80
    targetPort: 80
    # nodePort: 30001 used for NodePort type